[package]
name = "fronius-rust-api-client"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12", features = ["blocking"]}
serde = "1.0.210"
tokio = { version = "1", features = ["full"] }

